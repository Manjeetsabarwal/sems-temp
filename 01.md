# Kill process on port 3000
lsof -ti:3000 | xargs kill -9

# Or using fuser (alternative)
fuser -k 3000/tcp

# Check if port is in use
lsof -ti:3000


# Kill all processes on port 3000 and restart
lsof -ti:3000 | xargs kill -9 2>/dev/null; sleep 2; cd "/home/ratxen/Downloads/School Exam Management System/backend" && npm run start:dev &

login and signup 

Alternative commands (for future reference)
If you need to run this migration again or on another system, here are the options:
Option 1: Using the script (recommended)
cd backendexport DATABASE_PASSWORD=postgresbash src/database/apply-student-habits-migration.sh
Option 2: Direct psql command
cd backendPGPASSWORD=postgres psql -h localhost -p 5432 -U postgres -d school_exam_db -f src/database/v3-create-student-habits-table.sql
Option 3: If using different credentials
# Set your database passwordexport DATABASE_PASSWORD=your_passwordexport DATABASE_HOST=localhostexport DATABASE_PORT=5432export DATABASE_USER=postgresexport DATABASE_NAME=school_exam_db# Then run the scriptcd backendbash src/database/apply-student-habits-migration.sh


lsof -ti:3000 | xargs kill -9 2>/dev/null && echo "Killed process on port 3000" || echo "No process found on port 3000"

lsof -ti:5173 | xargs kill -9 2>/dev/null && echo "Killed process on port 5173" || echo "No process found on port 5173"



